import { styled } from "@/utils";

export const wrapper = styled(`
  hidden md:flex
  gap-12
`);

export const link = styled(`
  text-white
  text-base
  font-titillium
  font-semibold
  tracking-wider
`);
